# CRM

Built-in Qefro application. Runtime source: `examples/basic-crm`.

The CRM customer entity is named `CrmCustomer` so it can be installed next to restaurant `Customer`.

```bash
qefro app install crm
qefro dev --app crm
```
