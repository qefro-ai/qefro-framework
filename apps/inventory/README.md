# Inventory (V1.0 benchmark)

Minimal metadata app used to prove Qefro handles relationships, child tables, documents, workflows, and reports. It is not a complete ERP.

```bash
qefro app validate inventory
qefro app install inventory
qefro migrate --app inventory
qefro tenant app enable demo inventory
```
